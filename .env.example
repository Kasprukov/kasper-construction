# Copy to `.env` and fill in. All optional — the app degrades gracefully.

# Contact form backend (Formspree URL, your API, etc.). Without it, the form is demo-only.
VITE_FORM_ENDPOINT=

# Analytics — set ONE. Loads only after the user accepts cookies.
VITE_PLAUSIBLE_DOMAIN=
VITE_GA_ID=
